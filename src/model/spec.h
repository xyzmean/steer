/* Shared spec: both the compiler and the resolver read the SAME file.
 *
 * The resolver could have been handed its channels as command-line arguments by
 * the compiler, but then two programs would carry two ideas of what a channel is,
 * and a stale service definition would silently disagree with the config. One
 * parser, one source of truth — and they ship in one binary, so a version skew
 * between them is not expressible.
 */
#ifndef STEER_SPEC_H
#define STEER_SPEC_H
#include <stdint.h>
#include <stddef.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "platform.h"
/* Раньше остальных заголовков: struct err нужен объявлениям load_spec/registry_assign ниже, а
 * заголовок, введённый ПОСЛЕ первого упоминания `struct err *` в списке параметров, создал бы
 * второй, несвязанный тег с той же областью видимости в один прототип (6.2.1p2 в терминах
 * стандарта C) — и определение в parse.c/registry.c не совпало бы с этим прототипом типом. */
#include "err.h"
/* Таблица вида (src/kinds/kind.h): поле kind у struct output — указатель на неё, а предикаты out_*
 * ниже — вопросы к её битам. Заголовок объявляет struct output и struct spec только по имени. */
#include "kind.h"

#define MAX_CHANNELS 64
#define MAX_OUTPUTS  16
#define MAX_FROM     32
/* Локальных устройств в спеке. Столько же, сколько адресных записей в `from_default`, и по
 * той же причине: это две формы одного ответа на «кто наши клиенты», и разные пределы у них
 * означали бы, что один способ описать сеть богаче другого без всякого основания. */
#define MAX_LAN_DEV  32
/* Several lists can feed ONE channel. Enabling "youtube" and "google" must not force
 * two channels with two rules and two sets — they are one destination as far as
 * routing is concerned. Read as several files rather than concatenated into one by
 * the caller: on a box with 6MB of overlay, duplicating list bytes to express "and"
 * is a cost with nothing to show for it. */
/* Списков одного вида в правиле. Шестнадцати не хватало на очевидное желание: в каталоге
 * splify2 под сорок записей, и «отправить в туннель всё» упиралось в `too many entries in
 * list` уже на восьмом сервисе. Шестьдесят четыре покрывают весь каталог с запасом.
 *
 * Расти этому числу стало дёшево: пути больше не лежат в правиле массивом по 256 байт на
 * каждый (это было 8 КБ на правило и полмегабайта на все), а хранятся указателями на копии
 * строк. Шестьдесят четыре указателя на два вида — килобайт на правило. */
#define MAX_FILES    64

/* Диапазонов портов в правиле канала.
 *
 * Discord'у нужно два (50000-65535 и 19000-20000), шестнадцать — с запасом. Столько же,
 * сколько адресов в «кому», и по той же причине: это перечень, который человек пишет
 * руками, а не выгружает файлом, поэтому предел здесь стоит ради фиксированного бюджета
 * памяти слабого роутера, а не ради смысла. Хранится массивом прямо в правиле: шестнадцать
 * пар по два байта — это 64 байта на правило, дешевле указателя с выделением. */
#define MAX_PORTS    16

/* Протокол, которым канал ограничен.
 *
 * ANY — НУЛЬ, и это не случайность: спека, написанная до появления поля, обязана значить
 * ровно то, что значила, а `struct channel c = {0}` даёт именно «протокол не критерий».
 * Третьего значения «оба» здесь нет: «и TCP, и UDP» — это и есть ANY, потому что ни один
 * из них не отбрасывается. Слово `both` в спеке принимается, но означает ровно ANY (см.
 * parse_channels): это ЗАПИСАННОЕ умолчание для интерфейса, у которого в выпадающем списке
 * три пункта, а не третье поведение движка. */
enum chan_proto { CH_PROTO_ANY = 0, CH_PROTO_TCP, CH_PROTO_UDP };

/* Диапазон портов назначения. Одиночный порт — это lo == hi: одна форма во внутреннем виде
 * избавляет и разбор, и генератор правил от ветки «а это порт или диапазон». */
struct port_range { unsigned short lo, hi; };

/* Сужение канала по транспортному уровню: протокол и порты назначения.
 *
 * ЗАЧЕМ ЭТО ЕСТЬ. Списки itdoginfo/allow-domains приходят двоичными `.srs`, и `discord.srs`
 * — единственный в релизе, который описывает не домены: `network: udp` плюс
 * `ip_cidr 104.16.0.0/12` плюс `port_range 50000:65535, 19000:20000`. Взять его подсети и
 * отбросить порты нельзя — 104.16.0.0/12 это Cloudflare, и весь TCP к нему уехал бы в
 * туннель молча. Без этого измерения такой набор нечем выразить, и читатель `.srs`
 * отказывался от него целиком (см. шапку srs.c).
 *
 * ПОЧЕМУ ОДНОЙ СТРУКТУРОЙ, А НЕ ТРЕМЯ ПОЛЯМИ РЯДОМ. Протокол и порты — это ОДИН ключ, по
 * которому компилятор различает группы каналов и по которому вычисляется имя набора
 * nftables. Разнеси их по отдельным полям — и каждое место, которое сравнивает или передаёт
 * сужение (слияние групп в steer.c, имя набора здесь, резолвер в dnsd.c), пришлось бы
 * править при добавлении следующего измерения, а забытое место означало бы два канала,
 * молча поделивших один набор адресов. */
struct l4match {
    enum chan_proto proto;
    struct port_range ports[MAX_PORTS];
    size_t ports_n;
};

/* Сужения нет вовсе — то есть канал ведёт себя ровно как канал схемы 1. NULL считается
 * отсутствием сужения: так вызывающему, у которого сужения быть не может, не нужен
 * пустой объект на стеке. */
static inline int l4match_empty(const struct l4match *m) {
    return !m || (m->proto == CH_PROTO_ANY && m->ports_n == 0);
}

/* Вид выхода — модуль в src/kinds (kind.h, docs/architecture.md, раздел 2, правило 1). Что
 * такое каждый вид и чем он отличается от соседей, записано в шапке его файла: direct.c,
 * interface.c, vless.c, xsteer.c, zapret.c, tgws.c, awg.c. */

/* Что делать с трафиком выхода, когда ни одно его устройство не работает.
 *
 * Умолчание — DROP, и это не осторожность ради осторожности: канал заводят именно
 * для того, чтобы трафик НЕ шёл напрямую. Молча вернуть его на открытый путь в
 * момент поломки — значит нарушить единственное обещание выхода ровно тогда, когда
 * это опаснее всего, и человек об этом не узнает. Пусть лучше не работает заметно,
 * чем работает не туда незаметно. */
enum on_fail { FAIL_DROP, FAIL_DIRECT, FAIL_ZAPRET };

/* Устройства выхода в порядке предпочтения: первое здоровое побеждает. Список — это
 * приоритет, ровно как порядок каналов. */
#define MAX_DEVICES 16

/* Узлов подписки, выбранных в один выход kind=vless.
 *
 * Столько же, сколько устройств у выхода, и по той же причине, по которой у lan_devices
 * предел общий с from_default: это ОДНО понятие — список кандидатов, первый живой забирает
 * трафик, — записанное для разных видов выхода. У kind=interface кандидаты называются
 * устройствами, у kind=vless — номерами узлов подписки, и разные пределы означали бы, что
 * одна форма перебора богаче другой без всякого основания.
 *
 * Восемь — это ещё и предел терпения при подъёме: каждый неотвечающий кандидат стоит восьми
 * секунд таймаута, и подписка из двадцати шести узлов целиком превратила бы «выход не
 * поднялся» в три с половиной минуты тишины. Кто хочет перебирать всю подписку, тот не
 * перечисляет узлы вовсе — пустой список и есть «первый рабочий среди всех пригодных». */
#define MAX_NODE_SEL 16

/* Обфускация транспорта выхода: WireGuard поверх поддельного TCP (см. obfs.c).
 *
 * Не отдельный вид выхода, а свойство существующего, и это не вкусовщина. Неизвестный
 * `kind` старый движок отвергает целиком (parse_outputs), то есть спека, записанная
 * новым интерфейсом, положила бы маршрутизацию на не обновлённом роутере; неизвестный
 * КЛЮЧ он пропускает (js_skip). Плюс по смыслу выход остаётся тем же интерфейсом —
 * меняется только то, чем его эндпоинт доставляется до сервера.
 *
 * Ключами WireGuard движок не владеет и владеть не должен: они живут в
 * /etc/config/network, а здесь лежит ровно то, что нужно обфускатору. */
struct out_obfs {
    int on;
    char server[64];        /* адрес сервера обфускации, только литерал (см. spec.c) */
    int server_port;
    /* Куда смотрит `Endpoint` пира WireGuard. По умолчанию 127.0.0.1: наружу этот порт
     * торчать не должен — трафик к нему уже расшифрован для WireGuard. */
    char listen[64];
    int listen_port;
};

/* НАСТРОЙКИ ВИДОВ — по структуре на вид, в union внутри struct output (docs/architecture.md,
 * «Вид выхода»). Прежде все поля всех видов лежали рядом, и поле одного вида читал другой (путь
 * `conf` у awg жил в поле xsteer): у каждого вида теперь своё, и заполняет его только разбор его
 * модуля (src/kinds/<вид>.c). Читать поле чужого вида нельзя — там настройка того, чей это выход;
 * помощники спрашивают свою настройку у вида (out_vless, out_xsteer, out_tgws в kind.h). */
struct iface_cfg {
    struct out_obfs obfs;
};

struct vless_cfg {
    /* Откуда брать узлы. Подписка, а не один узел, потому что
     * failover между узлами — то же самое, что между устройствами, и списком он и
     * выражается. Файл, а не URL: скачивание это дело управляющего слоя, движок читает
     * то, что ему положили — см. правило про списки в README. */
    char sub_file[256];
    /* Какие узлы подписки брать и в каком порядке предпочтения. ПУСТОЙ список (умолчание)
     * означает «первый рабочий среди всех пригодных»: тогда выбор делает проверка при
     * подъёме, а не человек, угадывающий номер.
     *
     * Список, а не один номер, потому что между узлами подписки нужен ровно тот же перебор,
     * что между устройствами выхода: человек выбирает три локации из двадцати шести и
     * ставит их в порядке предпочтения, а живого выбирает движок. Одиночное `node` осталось
     * сокращением для списка из одного элемента — той же парой, что `device`/`devices`.
     *
     * Номера — среди ПРИГОДНЫХ узлов, то есть те же, что печатает `steer vless-nodes`. Одно
     * значение слова «номер узла» на весь проект (см. cmd_vless_nodes). */
    int nodes[MAX_NODE_SEL];
    size_t nodes_n;
};

struct xsteer_cfg {
    /* Путь к конфигурации в стиле wg. ПУТЬ, а не сами ключи, и
     * это принципиально: спеку читает и печатает целиком управляющий слой, она уходит в
     * status (его опрашивают раз в пять секунд), в diag и в резервную копию. Приватный
     * ключ в ней означал бы ключ, уже покинувший коробку. По умолчанию выводится из
     * имени выхода — держать два имени, которым позволено разойтись, незачем (то же
     * решение, что с device). */
    char conf[256];
    /* Режим потока: записи по НАСТОЯЩЕМУ TCP вместо поддельного (см.
     * src/proto/xsteer/xsstream.h). Нужен там, где сырой сокет недоступен — у провайдера, который его
     * режет, в контейнере без CAP_NET_RAW, — и там, где хаб держит только этот режим.
     * stream_port — порт хаба для потока; 0 означает «тот же, что в Endpoint». */
    int stream, stream_port;
};

struct awg_cfg {
    /* Путь к файлу awg-quick/wg-quick. Ключ спеки тот же, что у xsteer, — `conf`, и довод тот же:
     * ключи в спеке не живут. По умолчанию выводится из имени выхода (src/kinds/awg.c). */
    char conf[256];
};

struct zapret_cfg {
    /* Файл с ключами nfqws — по строке на ключ. ПУТЬ, а не сама
     * стратегия, и по той же причине, что у conf xsteer: спека печатается целиком в status,
     * в diag и в резервную копию, а стратегия обхода — это два десятка строк с путями к
     * файлам-подделкам, которым там не место. Путь по умолчанию выводится из имени выхода.
     *
     * Кто этот файл пишет — управляющий слой (splify2): он знает каталог стратегий, а
     * движок знает только, что файл надо отдать процессу. Тот же уговор, что со списками
     * адресов: скачивание — не дело движка. */
    char opts[256];
};

struct tgws_cfg {
    /* Домен точек веб-сокета: `kwsN.<домен>`. Чужой по природе (домен владельца
     * роутера за Cloudflare либо общественный), поэтому поле, а не константа — см. tgws.c. */
    char domain[128];
};

/* Что спека написала в ключах видов — до того, как известен вид (ключ `kind` может стоять в
 * объекте последним). Разбирает их parse.c для всех видов сразу, вид получает готовые значения
 * (kind_ops.parse) и решает, что они значат; общий разбор по битам kind_ops.keys отказывает
 * ключу чужого вида. */
struct out_keys {
    struct out_obfs obfs;       /* obfs.on — ключ задан и разобран */
    char sub_file[256];
    char conf[256];
    char opts_file[256];
    char domain[128];
    int stream, stream_port;
    int nodes[MAX_NODE_SEL];
    size_t nodes_n;
    /* Какой из двух форм записан выбор узлов: `node` (сокращение) или `nodes` (список). */
    int node_one, node_many;
};

struct output {
    char name[32];
    /* Вид — таблица функций своего модуля (src/kinds/kind.h). Указатель, а не номер в реестре:
     * общий код зовёт kind_of(o)->health и соседей прямо, без поиска, а новому виду не нужно
     * место в общем перечне номеров — только свой файл и строка в профиле. NULL (обнулённый
     * выход — так их собирают стенды) читается как direct: kind_of ниже, тот же смысл, что был
     * у нулевого значения прежнего перечня. */
    const struct kind_ops *kind;
    /* Активное устройство — то, через которое трафик идёт СЕЙЧАС. Отдельно от списка
     * кандидатов, потому что failover меняет его, не трогая настройку. */
    char device[32];
    char devices[MAX_DEVICES][32];
    size_t devices_n;
    enum on_fail on_fail;
    /* ЧЕРЕЗ КАКОЙ ВЫХОД ИДЁТ СОБСТВЕННЫЙ ТРАФИК ТУННЕЛЯ ЭТОГО ВЫХОДА (`via` в спеке).
     * Пусто — напрямую, как всегда. Имя, а не указатель: спека разбирается целиком до
     * проверок, и выход-цель может стоять в ней ниже. Смысл, проверки и метка — в блоке
     * «вложенные выходы» ниже (out_via, out_underlay_mark). */
    char via[32];
    /* ВТОРАЯ ОСЬ У СТОРОЖА: выбирать не только «живо», но и «насколько быстро».
     *
     * До запуска 65 порядок в `devices`/`nodes` был единственным мерилом: сторож брал
     * ПЕРВОЕ здоровое и на этом останавливался (и останавливался нарочно — пробить пробой
     * каждого мёртвого запаса стоит таймаут на каждом тике). То есть машина отвечала на
     * вопрос «жив ли», и ни в одной точке — на вопрос «насколько хорош».
     *
     * `prefer: latency` включает второй вопрос. НЕ ПО УМОЛЧАНИЮ, и это про совместимость:
     * старый движок неизвестный ключ пропускает (js_skip), то есть ведёт себя как сегодня —
     * а сегодня это «по порядку». Ключ, включённый по умолчанию, у него бы молча не
     * действовал, и человек считал бы, что выбор идёт по замеру, когда он идёт по списку.
     *
     * Цена названа там, где она возникает (failover.c): замер требует опросить ВСЕХ
     * кандидатов, а не остановиться на первом живом, поэтому идёт он на своём, длинном
     * интервале, а между замерами сторож работает как прежде. Числа взяты у sing-box,
     * где эта задача решена давно: интервал 3 минуты, допуск 50 мс. */
    int prefer_latency;
    int lat_tolerance_ms;   /* 0 — умолчание LAT_TOLERANCE_MS */
    int lat_interval_s;     /* 0 — умолчание LAT_INTERVAL_S  */
    /* Настройка вида — ровно одна из этих, по kind. */
    union {
        struct iface_cfg iface;
        struct vless_cfg vless;
        struct xsteer_cfg xs;
        struct awg_cfg awg;
        struct zapret_cfg zp;
        struct tgws_cfg tg;
    };
    uint32_t mark;      /* 0 for direct: claiming a packet needs no mark */
    int table;
};

struct channel {
    char name[32];
    char out[32];
    /* Пути — указатели на копии строк, живущие до конца процесса: спека разбирается один
     * раз, освобождать их некому и незачем. Массив фиксированных буферов стоил бы 32 КБ на
     * правило при нынешнем пределе. */
    const char *prefixes_files[MAX_FILES];
    size_t prefixes_n;
    const char *domains_files[MAX_FILES];
    size_t domains_n;
    /* Наборы правил sing-box (`.srs`): и имена, и подсети, и сужение в одном файле. Читает их
     * не разбор спеки, а те, кому нужно содержимое: компилятор (подсети и сужение — src/compile/
     * groups.c) и резолвер (имена — src/dnsd/table.c); общий читатель — src/model/srs.c. */
    const char *srs_files[MAX_FILES];
    size_t srs_n;
    /* fake-IP (default) or real-IP for a domain channel. See dnsd.c: fake-IP is
     * precise per domain but makes every traceroute hop show the fake address,
     * because the kernel rewrites ICMP errors to look like they came from the
     * address the client addressed. real-IP keeps hops legible and loses precision
     * only where two domains share one backend address. */
    int realip;
    char from[MAX_FROM][64];
    size_t from_n;
    /* СХЕМА 2: правило принадлежит ОДНОМУ УСТРОЙСТВУ и потому старше любого глобального.
     *
     * ЗАЧЕМ ОТДЕЛЬНЫЙ ПРИЗНАК, если `from` и так умеет и адрес, и MAC. Порядок совпадения
     * читается со спеки: побеждает первое совпавшее правило, и правило на телефон работало
     * ровно до тех пор, пока человек держал его выше глобальных. Стоило добавить новое
     * правило «весь Instagram в туннель» и поставить его первым — и исключение для телефона
     * перестало действовать, молча. То есть приоритет был свойством ПОРЯДКА В СПИСКЕ, а не
     * свойством правила, и удержать его должен был человек.
     *
     * С этим признаком порядок обеспечивается построением: build_groups проходит спеку
     * ДВАЖДЫ и кладёт устройства первыми, как бы они ни лежали в спеке. Правило на телефон
     * старше глобального всегда — это и просили.
     *
     * ЧТО ТРЕБУЕТСЯ ОТ ТАКОГО ПРАВИЛА: непустой `from`, и в нём только ОДИНОЧНЫЕ хозяева —
     * адрес без маски (или /32) либо MAC. Подсеть в правиле «на устройство» означала бы, что
     * приоритет получил не телефон, а половина сети, — и получила бы его тихо, потому что
     * снаружи такое правило выглядит точно так же.
     *
     * ПОБОЧНОЕ СЛЕДСТВИЕ, ради которого это и просили: у такого правила `any` законен без
     * `allow_all`. Общий запрет заведён против описки «весь трафик в туннель», у которой
     * цена — все клиенты без интернета и починка с провода; у правила на одного хозяина
     * цена ошибки — один хозяин, и он же её и заметит. Поэтому «весь трафик этого телефона
     * в туннель» и «этот ноутбук не маршрутизируем вовсе» (`out` прямого выхода плюс `any`)
     * записываются без лишнего согласия. */
    int dev_scope;
    int any;
    /* Явное согласие на канал, который забирает весь трафик. Без него `any` без
     * списков отвергается: это почти всегда описка, а последствие — клиенты
     * теряют и роутер, и DNS, то есть чинить придётся с провода. */
    int allow_all;
    /* СХЕМА 2: сужение по протоколу и портам назначения. Пусто — канал схемы 1, и
     * поведение у него прежнее до последнего байта текста правил. */
    struct l4match l4;
    /* Стояли ли в `match` ключи схемы 2. Отдельно от самого сужения, и это не дублирование:
     * `"proto": "both"` — записанное умолчание, сужения из него не выходит, а ключ схемы 2 в
     * спеке `schema: 1` обязан быть отказом. Выводи признак из непустоты сужения — и `both`
     * проходил бы там, где `udp` отвергается, то есть человек читал бы это как «порты в
     * первой схеме работают», и однажды написал бы рядом настоящий порт. */
    int l4_written;
    /** Выключенное правило: лежит в спеке, но в правила не превращается.
     *
     *  Нужно потому, что «отключу на вечер» — самая частая просьба, а единственным способом
     *  было удалить канал вместе с выбранными списками и собрать его заново. Интерфейс до
     *  этого поля обходился так: вынимал канал из спеки и держал у себя. Работало, но канал
     *  становился невидим движку — ни `status`, ни `explain` о нём не знали, и «почему сайт
     *  не идёт» приходилось объяснять тем, чего в спеке нет.
     *
     *  Умолчание — включён: спека, написанная до этого поля, обязана значить то же, что
     *  значила. Поэтому в JSON поле называется `enabled` и проверяется на `false`, а внутри
     *  хранится обратное — так поле, которого нет, даёт нуль и означает «работает». */
    int disabled;
};

/* СПЕКА — ЗНАЧЕНИЕ, А НЕ ГЛОБАЛЫ (docs/architecture.md, раздел 2, правило 6).
 *
 * Раньше разобранная спека жила в пяти глобальных массивах (g_out, g_ch, g_from_default,
 * g_lan_dev, g_traceroute_hops): один экземпляр на процесс, доступный отовсюду без
 * объявления. Решение владельца — отказаться от этого в пользу явной передачи параметром:
 * struct spec собирает те же пять групп полей в одно значение, load_spec заполняет его, а
 * каждая функция, которой нужна спека, получает указатель на него параметром.
 *
 * ПОЧЕМУ НЕ ОДИН ОБЩИЙ ЭКЗЕМПЛЯР ВМЕСТО ПЯТИ ГЛОБАЛОВ. Это был бы тот же порок под другим
 * именем: g_spec вместо g_out теряет ту же информацию — кто читает спеку, а кто нет, — и
 * даёт любой функции доступ к ней без спроса. Стенд, собирающий failover.c без парсера
 * (failovermatch), или два процесса с разными спеками в одном тесте оказались бы вынуждены
 * либо делить один экземпляр, либо снова заводить глобал под другим именем. Явный параметр
 * делает то же самое видимым в сигнатуре: функция, которая спеку не получает, спеку не
 * читает — это проверяет компилятор, а не память того, кто её писал.
 *
 * ГДЕ ЖИВЁТ ЭКЗЕМПЛЯР. struct spec большой (g_ch один — под 200 КБ при MAX_CHANNELS=64), и
 * на стеке его не держат: каждая точка входа (cmd_* в daemon/, dnsd, tgws, vless-команды,
 * awg-команды) заводит свой `static struct spec` и передаёт указатель вниз по вызовам. Один
 * процесс — один экземпляр за время своей жизни, но эти экземпляры не делят состояние между
 * собой, и это и есть разница со старыми глобалами: явную, а не подразумеваемую. */
struct spec {
    struct output out[MAX_OUTPUTS];
    size_t out_n;
    struct channel ch[MAX_CHANNELS];
    size_t ch_n;
    char from_default[MAX_FROM][64];
    size_t from_default_n;
    /* Локальные устройства: те, с которых движок забирает трафик клиентов.
     *
     * СПИСОК, А НЕ СТРОКА, потому что роутер бывает выходной точкой не только для своего
     * моста: у splify2 просили маршрутизировать ещё и хостов из Tailscale/ZeroTier, для
     * которых он шлюз (splify2#16). В спеке это `lan_devices`; прежнее `lan_device` осталось
     * сокращением для списка из одного элемента — той же парой, что `device`/`devices` у
     * выхода.
     *
     * ПОЧЕМУ ВЫБОР ПО УСТРОЙСТВУ, А НЕ ПО ПОДСЕТИ. Раньше движок выводил подсеть клиентов из
     * адреса единственного устройства. Для Tailscale это не работает в принципе: у
     * tailscale0 на роутере адрес обычно /32, и выведенная из него «подсеть» — сам роутер.
     * Имя устройства отвечает на тот же вопрос точнее и там, где адрес не отвечает вовсе:
     * заодно в правило попадают клиенты за вторым роутером в LAN, у которых адреса чужой
     * подсети, а интерфейс тот же. Умолчание — один "br-lan", выставляется в load_spec. */
    char lan_dev[MAX_LAN_DEV][64];
    size_t lan_dev_n;
    int traceroute_hops;
};
/* Каталог состояния (steer_state_dir) и каталог имён таблиц iproute2 (steer_rt_tables_dir) —
 * пути платформы со швом переопределения: src/platform/platform.h. */

/* The port `steer dnsd` listens on and the redirect points at. One constant, so
 * the two halves cannot disagree about where DNS is being steered. */
#define DNS_PORT 5300

/* Вид выхода и его свойства. kind_of — единственное место, где читается поле kind: обнулённый
 * выход (kind == NULL) — это direct, как было у нулевого значения прежнего перечня видов. */
static inline const struct kind_ops *kind_of(const struct output *o) {
    return o->kind ? o->kind : KIND_ZERO;
}
static inline unsigned out_caps(const struct output *o) {
    const struct kind_ops *k = kind_of(o);
    return k->caps | (k->caps_of ? k->caps_of(o) : 0);
}
static inline int out_has_cap(const struct output *o, unsigned cap) { return (out_caps(o) & cap) != 0; }

/* Выход, у которого есть устройство и своя таблица маршрутизации. vless сюда входит:
 * его TUN — такое же устройство, и вся логика меток, маршрутов и проверок к нему
 * применима без изменений. Одна функция вместо повторения условия в пяти местах —
 * иначе добавление нового вида требовало бы найти их все, а забытое место означало бы
 * выход, который настроен, но не маршрутизируется. */
static inline int out_has_device(const struct output *o) { return out_has_cap(o, KC_DEVICE); }

/* Выход, которому нужна СВОЯ МЕТКА, чтобы его трафик можно было узнать в ядре.
 *
 * Не то же самое, что out_has_device, и разошлись они на kind=zapret: у того устройства нет
 * и таблицы маршрутизации нет (пакет уходит обычным маршрутом), а метка есть — по ней
 * цепочка postrouting отдаёт пакет нужному экземпляру nfqws. Пока условие было одно на две
 * надобности, добавление такого вида означало бы выход, помеченный ничем: правило канала
 * встало бы, а трафик в очередь не попал бы, и понять это было бы нечем — правила выглядят
 * применёнными.
 *
 * direct метки не получает по-прежнему: заявлять пакет ничем не нужно, он и так идёт туда. */
static inline int out_needs_mark(const struct output *o) { return out_has_cap(o, KC_MARK); }

/* Выход, которому нужна метка СОЕДИНЕНИЯ, а не только метка пакета.
 *
 * Отдельно от out_needs_mark, потому что метка соединения живёт НЕ В НАШЕМ ФАЙЛЕ И НЕ В
 * НАШЕЙ ТАБЛИЦЕ. Она лежит в записи conntrack ядра и переживает и перезапуск службы, и
 * `nft delete table`, и удаление пакета целиком — снять её оттуда нечем, кроме как выждав
 * срок жизни записи. Значит ставить её можно только тем выходам, которым она ДЕЙСТВИТЕЛЬНО
 * нужна, а не всем подряд «на всякий случай».
 *
 * Кому нужна. Выходу kind=zapret — по ней цепочка postrouting узнаёт соединение после
 * хука forward, где метку пакета переписывают чужие (Tailscale). Выходам с устройством —
 * из-за выгрузки потоков: после установления соединения наша цепочка видит единицы пакетов
 * вместо тысяч, и пересмотреть маршрут (или применить запрет on_fail=drop) можно только по
 * метке соединения.
 *
 * Кому НЕ нужна — kind=tgws, и это не экономия, а починенная поломка. Перехват стоит в nat
 * prerouting и решает по метке ПАКЕТА, которую ставит наша же цепочка на десять приоритетов
 * раньше; ни таблицы маршрутизации, ни очереди у такого выхода нет, а соединение
 * заканчивается на самом роутере — то есть в forward не попадает и выгружено быть не может.
 * Метка соединения ему не нужна была никогда, а вреда приносила много: к нашему биту
 * добавляется ZAPRET_SKIP_MARK (см. out_skips_zapret), и он означает «этот трафик не твой»
 * для системного обхода. После удаления микропакета tgws правила исчезают, а записи
 * conntrack с этим битом остаются — и подхватившие Telegram podkop или splify2 получают
 * трафик, уже помеченный «мимо обхода». Снаружи это ровно то, о чём сказал владелец:
 * «удалил stgws — другие сервисы не могут нормально маршрутизировать Telegram». */
static inline int out_needs_ctmark(const struct output *o) { return out_has_cap(o, KC_CTMARK); }

/* Выход, устройство которого создаёт и обслуживает НАШ процесс, а не netifd.
 *
 * Отдельно от out_has_device, потому что из этого следуют другие вещи: ждать устройство
 * снаружи нельзя (его ещё нет), ifdown/ifup бесполезны (netifd про него не знает —
 * «Interface … not found», и на живом роутере это вечный холостой цикл в журнале), а
 * поднимает замену procd. */
static inline int out_engine_managed(const struct output *o) { return out_has_cap(o, KC_ENGINE_OWNED); }

/* Выход, которому masquerade не нужен: наружу он ходит от своего имени.
 *
 * Причины у vless и xsteer РАЗНЫЕ, и объединять их формулировкой нельзя — только
 * следствием. У vless адреса клиентов границу не переходят вовсе: клиент завершает TCP у
 * себя и соединяется с сервером обычным сокетом. У xsteer они переходят, но переходят к
 * хабу внутри туннеля, где транслировать их нечем и не нужно; более того, NAT там ВРЕДЕН
 * — он скрывает, от какой пира пришёл пакет, и ломает обратный поиск по AllowedIPs.
 * Отсюда одна функция для решения и разные тексты в сообщениях. */
static inline int out_self_natting(const struct output *o) { return out_has_cap(o, KC_SELF_NAT); }

/* Выход, трафик которого общий обход DPI трогать НЕ должен.
 *
 * Это все виды, кроме kind=direct, и исключение здесь ровно одно — оно же и есть смысл
 * прямого канала: там общий обход обязан работать, для этого канал и заводят.
 *
 * ПОЧЕМУ ОСТАЛЬНЫМ ОБХОД ВРЕДЕН, а не просто бесполезен. Пакет, уходящий в туннель, DPI
 * видит снаружи как сам туннель: разбирать его полезную нагрузку невозможно, и стратегия
 * обхода работает не с тем, для чего написана. Но nfqws при этом рассинхронизирует и режет
 * ВНЕШНИЕ пакеты — то есть трогает транспорт туннеля, и рукопожатие может не сойтись. Плюс
 * лишний проход через очередь на каждый пакет.
 *
 * Мост Telegram (kind=tgws) здесь по той же причине: его трафик разбирает наш собственный
 * обработчик, и второй разбиратель поверх — это два обхода на один пакет, то есть не то,
 * что выбрал человек, ни в одном из двух смыслов.
 *
 * ОДНОЙ ФУНКЦИЕЙ, а не `case` по kind на месте: спрашивают об этом генератор правил и
 * (со временем) диагностика, а две копии перечня разошлись бы при появлении следующего
 * вида выхода — и разошлись бы молча, потому что правила у обоих выглядят исправно. */
static inline int out_skips_zapret(const struct output *o) {
    /* На телефоне zapret нет (решение владельца: «zapret не надо», .zapret = 0 в
     * src/platform/android.c) — ни системного, которому бит говорит «не твоё», ни своего
     * kind=zapret (спека его отвергает, см. src/kinds/zapret.c). Бит ставить некому читать, а
     * стоит он в бите 30 — там, где netd отдаёт производителю два бита метки (Fwmark.h:
     * vendor, 29-30). Ноль здесь снимает и бит в правилах разметки, и цепочку failopen,
     * которая существует только ради него (out_failopen_capable ниже). */
    if (!plat()->zapret) return 0;
    return out_has_cap(o, KC_SKIP_ZAPRET);
}

/* ВЫХОД УПАЛ, ТРАФИК ПУЩЕН НАПРЯМУЮ — И ОН СНОВА ОБЫЧНЫЙ ТРАФИК РОУТЕРА.
 *
 * Бит ZAPRET_SKIP_MARK ставит правило разметки в prerouting_mark, и ставит БЕЗУСЛОВНО: оно
 * ничего не знает о том, жив ли туннель. Пока выход работает, так и надо (см. out_skips_zapret
 * выше). Но при отказе с on_fail=direct или on_fail=zapret сторож снимает правило fwmark, и
 * помеченный пакет уходит по таблице main — через WAN, открытым путём, — а бит на нём
 * остаётся. Общий обход видит «не мой» и такой пакет не трогает. То есть «напрямую» на деле
 * значило «напрямую и мимо zapret»: сайты, которые без обхода не открываются, у человека с
 * упавшим туннелем не открывались и там, где весь остальной роутер их открывает. А у
 * on_fail=zapret, который ровно обходом и обещает, это было прямое расхождение с обещанием.
 *
 * Выбранное решение: СОСТОЯНИЕ «УПАЛ И ПУЩЕН НАПРЯМУЮ» ЛЕЖИТ В ЯДРЕ, в наборе FAILOPEN_SET
 * нашей таблицы (метки выходов), а отдельная цепочка prerouting_failopen сразу после
 * разметки (mangle + 2) снимает бит с пакетов, чья метка в наборе. Метку в набор кладёт и
 * вынимает тот, кто и так меняет маршрут выхода: сторож в apply_failed (кладёт при
 * direct/zapret, вынимает при drop) и в bind_device (вынимает при привязке к живому
 * устройству), apply — когда устройства для выхода нет вовсе (apply_routing в steer.c). Сверка
 * сторожа это же и чинит: apply пересоздаёт таблицу с пустым набором, но заодно возвращает
 * правило fwmark, и первый же проход видит у упавшего выхода расхождение и зовёт apply_failed.
 *
 * Почему не «ставить бит только при живом выходе» прямо в правиле разметки: правило одно на
 * группу каналов и знает о выходе только его метку, а переписывать правила на каждом отказе
 * значило бы пересобирать цепочку из сторожа. Почему не по устройству в postrouting («бит —
 * только тем, кто уходит в туннель»): список устройств у выхода меняется, у vless и xsteer
 * устройство заводит наш же процесс, и промах в перечне означал бы тихо вернувшийся обход
 * внешних пакетов туннеля. Набор же отвечает ровно на вопрос «пущен ли выход напрямую», и
 * ответ на него уже есть у сторожа.
 *
 * Только выходы с устройством (interface, vless, xsteer): у kind=zapret on_fail выражает
 * флаг bypass очереди (см. generate), у tgws on_fail бывает только drop. Цепочка и набор
 * ставятся, лишь если в спеке есть такой выход с on_fail не drop, — остальным на каждый
 * пакет лишний поиск ни к чему. */
#define FAILOPEN_SET "failopen"
static inline int out_failopen_capable(const struct output *o) {
    return out_has_device(o) && out_skips_zapret(o) && o->on_fail != FAIL_DROP;
}

/* Похожа ли строка списка на адресную запись: «10.0.0.1», «10.0.0.0/8» или диапазон
 * «10.0.0.1-10.0.0.9». Всё остальное непустое и не комментарий — доменное правило.
 *
 * Одна функция на двух читателей одного файла: компилятор берёт отсюда адреса, резолвер —
 * домены. Подробнее — у определения в spec.c. */
int spec_line_is_addr(const char *s);

/* Одиночный хозяин: адрес, адрес/32 или MAC. Подсеть — нет. Нужно правилу на устройство,
 * см. поле dev_scope у канала. */
int spec_one_host(const char *s);

/* «КТО» НА САМОМ УСТРОЙСТВЕ: трафик, который телефон порождает сам, а не пропускает через себя.
 *
 * Канал в `from` называет или клиентов раздачи (адреса, MAC — как на роутере), или самого себя:
 *   "self"        — трафик всех приложений телефона (UID от 10000, см. STEER_APP_UID_MIN);
 *                   системные демоны и сам движок идут напрямую;
 *   "uid:N"       — трафик приложения с UID N;
 *   "uid:N-M"     — диапазон UID (одно приложение у нескольких пользователей устройства:
 *                   UID = пользователь * 100000 + номер приложения).
 * Смешивать в одном канале себя и клиентов нельзя — разные хуки ядра (output и prerouting), и
 * правило пришлось бы разбить на два. Только на телефоне (plat()->local_channels): на роутере
 * собственного трафика, который стоило бы маршрутизировать каналом, нет, и движок его не
 * касается.
 *
 * Демоны исключены из "self" нарочно: под root работает сам движок (клиент VLESS, резолвер,
 * пробы сторожа), и «весь телефон в туннель» иначе завернул бы в туннель сам туннель; под
 * своими UID — clatd и прочие системные службы сети. DNS приложений идёт от DnsResolver (root)
 * и маршрутизируется не каналом, а заворотом к резолверу движка — см. STEER_SELF_MARK. */
int from_is_local(const char *s);
/* Разобрать "uid:N" или "uid:N-M". 0 — годится, lo/hi заполнены; -1 — нет. */
int from_uid_range(const char *s, unsigned *lo, unsigned *hi);

/* Годен ли идентификатор из спеки к подстановке в командную строку и в имя набора.
 * Проверяется парсером при загрузке — см. развёрнутое объяснение у определения. */
int name_ok(const char *s);
/* Годна ли ПОДПИСЬ (имя канала) к выводу в JSON и в текст ruleset. Мягче name_ok:
 * разрешает любой UTF-8, запрещает кавычку, обратную косую и управляющие символы. */
int label_ok(const char *s);

/* Имя набора nftables для группы каналов. Вычисляют двое — компилятор и резолвер, — и
 * функция общая именно поэтому: разойдись они в имени, резолвер наполнял бы набор,
 * которого нет. kind — "ip", "dom" или "all". Подробности у определения в spec.c.
 *
 * l4 в списке параметров по той же причине, по которой там уже стоят from и realip: это
 * ещё один признак, по которому компилятор РАЗДЕЛЯЕТ группы, а значит и наборы. NULL —
 * «сужения нет», и тогда имя получается прежним, байт в байт. */
void group_set_name(const struct spec *sp, char *dst, size_t n, const char *out, const char *kind,
                    const char (*from)[64], size_t from_n, int realip,
                    const struct l4match *l4);
/* Одно ли сужение у двух каналов. Нужна компилятору: протокол и порты входят в ключ
 * слияния групп наравне с выходом и списком клиентов — см. build_groups в steer.c. */
int l4match_same(const struct l4match *a, const struct l4match *b);
/* Имена наборов канала с наборами .srs: составного (смешанное сужение) и доп. группы (клаузы с
 * условиями, которых у канала нет) — см. parse.c и src/model/srsplan.c. */
void group_set_name_mixed(const struct spec *sp, char *dst, size_t n, const char *out,
                          const char *kind, const char (*from)[64], size_t from_n, int realip);
void group_set_name_extra(const struct spec *sp, char *dst, size_t n, const char *out,
                          const char *kind, const char (*from)[64], size_t from_n, int realip,
                          unsigned id);
/* Разбор спеки — правило 5 (docs/architecture.md, раздел 2): модель ошибку ВОЗВРАЩАЕТ, а не
 * завершает процесс сама. 0 — разобрано, *s заполнен (см. правило 6 у struct spec выше); -1 —
 * отказ, текст в e->msg. Завершает процесс только вызывающий, дошедший до точки входа:
 * `static struct spec spec; struct err e; if (load_spec(path, &spec, &e) < 0) err_die(&e);`. */
int load_spec(const char *path, struct spec *s, struct err *e);
struct output *out_by_name(const struct spec *sp, const char *n);

/* Кому ПРИНАДЛЕЖИТ устройство: выход, чей процесс его создал (vless, xsteer). NULL, если
 * такого нет, — тогда устройством владеет система, и отвечает за него сам выход.
 *
 * Заведено потому, что одно и то же устройство может быть названо ДВАЖДЫ: сначала выходом,
 * который его создаёт, а потом — в `devices` другого выхода, собирающего из разнородных
 * туннелей пул с порядком предпочтения (локация подписки, wireguard, хаб xsteer в одном
 * списке). Так его собирает интерфейс splify2, и так его может написать человек руками.
 *
 * Всё, что решается ВИДОМ выхода, но относится к устройству, обязано спрашивать здесь, а не
 * у выхода, который устройство назвал: мера здоровья и способ оживления (failover.c), нужен
 * ли masquerade (apply и diag в steer.c). Иначе у пула kind=interface с устройством
 * VLESS-туннеля проба была бы ICMP, который через него не идёт принципиально, а диагностика
 * требовала бы masquerade, которого этому устройству не нужно.
 *
 * Ищется только среди выходов out_engine_managed: устройство, которым владеет netifd, ничьей
 * собственностью здесь не считается, и обычный wireguard в пуле проверяется как прежде.
 * Совпадение имён (выход kind=xsteer, названный «wg0» при живом интерфейсе wg0) остаётся
 * ошибкой настройки: два разных устройства с одним именем система и сама не различит.
 *
 * Объявлена здесь, а определена в failover.c — тот же случай, что bind_device: спрашивают её
 * из трёх мест, а живёт она там, где появилась первая нужда. Переносить определение в spec.c
 * нельзя без потерь: стенд failovermatch компилирует failover.c со своими выходами и без
 * парсера, и копия функции в стенде стала бы вторым источником правды, который разойдётся с
 * первым молча. */
const struct output *device_owner(const struct spec *sp, const char *dev);
/* Тот же вопрос с готовым запасным ответом: владелец устройства, а если владельца нет — сам
 * выход. Отдельно от device_owner, потому что все три места зовут именно эту форму, и
 * `owner ? owner : o` в каждом из них — три шанса забыть его в одном. */
const struct output *out_for_device(const struct spec *sp, const struct output *o, const char *dev);

/* Заполнить `device` каждого выхода тем устройством, через которое трафик идёт СЕЙЧАС.
 *
 * Зовут её команды, читающие спеку своим процессом: apply — перед привязкой таблиц, status
 * и diag — перед тем, как рассказывать о выходе. Без неё `device` у них означает первого
 * кандидата, то есть предпочтение, а не факт, и пул с рабочим запасным устройством
 * выглядит сломанным. Порядок выбора и его доводы — у определения в failover.c. */
void outputs_adopt_active(struct spec *sp);

/* ЗАПАСНОЙ ЗАПРЕТ В ТАБЛИЦЕ ВЫХОДА С on_fail=drop: `blackhole default metric <это>`.
 *
 * Маршрут выхода — `default dev X` — ссылается на устройство, и когда устройство исчезает, ядро
 * вычищает маршрут само, в тот же миг: TUN помощника пропадает вместе с процессом (supervise
 * перезапускает его через пять секунд и дольше), устройство awg пересоздаётся при смене модуля,
 * интерфейс падает. Таблица становится пустой, а пустая таблица — это не «нет пути», а «ищи
 * дальше»: помеченный пакет уходит по main, напрямую, и так до следующего прохода сторожа (до
 * минуты) или до привязки нового TUN. Для выхода, который обещал «лучше ничего, чем мимо
 * туннеля», это утечка, и её не закрыть никаким порядком команд — её создаёт ядро.
 *
 * Поэтому у такого выхода в таблице всегда лежит второй маршрут по умолчанию — запрет с
 * наибольшей метрикой. Пока есть маршрут в устройство (метрика 0), решает он; исчез — остаётся
 * запрет, и пакет отбрасывается, а не утекает. Сверка сторожа запасной запрет за «запрет в
 * таблице» не считает (иначе живой выход выглядел бы упавшим), а у выхода с direct/zapret его
 * нет вовсе: пустота там — обещанное поведение. Число — любое заметно большее нуля; взято
 * круглое, чтобы строку `ip route show` было видно глазами. */
#define STEER_BACKSTOP_METRIC 65535

/* ---- вложенные выходы: `via` ----------------------------------------------------------
 *
 * ЗАЧЕМ. Туннель одного выхода бывает нужно пустить ВНУТРИ другого: WireGuard, у которого UDP
 * к серверу режут, — внутри VLESS (пример владельца: выход awg с `via` на выход vless), или
 * наоборот, VLESS, чей сервер виден только из сети WireGuard, — внутри интерфейса. Каналы
 * здесь ни при чём: они решают, в какой выход идёт трафик КЛИЕНТОВ, а `via` решает, куда
 * идёт трафик САМОГО ТУННЕЛЯ — его соединение с сервером.
 *
 * КАК. Ровно тем же механизмом, которым движок и так ведёт трафик в выход: меткой. У выхода-
 * цели уже есть своя метка, ip rule «fwmark метка/маска → таблица выхода» и маршрут по
 * умолчанию в его устройство. Сокет наверх внутреннего выхода получает метку выхода-цели
 * (SO_MARK у клиентов vless, xsteer и обфускатора; WGDEVICE_A_FWMARK у выхода awg), и ядро
 * уводит его пакеты в таблицу цели при поиске маршрута — ещё до того, как пакет родился:
 * connect() на помеченном сокете выбирает и устройство, и адрес источника по таблице цели.
 * Новых правил ip rule для этого не нужно, и каналы трафик туннеля не трогают — он локальный
 * (хук output), а каналы роутера живут на prerouting, каналы телефона — на output только для
 * приложений (skuid от 10000), а помощники движка работают под root. Вывод generate() от `via`
 * зависит в одном месте, и только на телефоне: заворот DNS приложений на output берёт и root
 * (DnsResolver), поэтому туннель пропускается по своему биту — см. STEER_TUNNEL_BIT.
 *
 * ЧТО С ОТКАЗОМ. Если выход-цель лёг, внутренний туннель по нему не доедет — поэтому сторож
 * считает внутренний выход нерабочим, пока лежит цель, и применяет ЕГО on_fail к его
 * каналам (failover.c). Проб и таймеров ради этого не заводится: ответ «цель жива» у сторожа
 * уже есть в том же проходе, выходы просто обходятся в порядке зависимостей. Сам транспорт
 * внутреннего туннеля при этом подчиняется on_fail ЦЕЛИ: при drop в её таблице blackhole и
 * соединение не встанет, при direct её правило снято и транспорт уйдёт обычным путём.
 *
 * Глубина цепочки — не больше MAX_VIA_DEPTH переходов: каждый слой — это заголовки и
 * шифрование ещё раз, и цепочка длиннее трёх означает скорее описку, чем замысел. */
#define MAX_VIA_DEPTH 3

/* Выход, у которого ЕСТЬ СВОЙ СОКЕТ НАВЕРХ, — только ему `via` и имеет смысл: метку ставит тот,
 * кто открывает соединение с сервером. vless и xsteer — наш процесс; interface с obfs — наш
 * обфускатор (его сырой сокет к серверу обфускации несёт UDP WireGuard); awg — ядро WireGuard,
 * но настраивает его движок, и метку сокета туннеля он задаёт сам (WGDEVICE_A_FWMARK, src/kinds/awg.c).
 * Обычный interface — НЕТ: его сокет открывает ядро WireGuard по настройке netifd, и метку ему
 * ставить не нам. Бит KC_OVER: у interface он зависит от obfs и приходит из caps_of вида. */
static inline int out_via_capable(const struct output *o) { return out_has_cap(o, KC_OVER); }

/* Годится ли выход в цели `via`: у него должны быть устройство и таблица маршрутизации, иначе
 * метке некуда вести. Это ровно out_has_device (interface, vless, xsteer, awg) — direct, zapret
 * и tgws отпадают (у первых двух
 * пакет уходит обычным маршрутом, у tgws перехват стоит на prerouting и трафика самого роутера
 * не касается). Отдельным именем, а не прямым вызовом, чтобы смысл читался в проверке. */
static inline int out_via_target_ok(const struct output *o) {
    return out_has_device(o);
}

/* Выход-цель `via` или NULL, если его нет. Поиск по sp->out, а не out_by_name: этот заголовок
 * включают стенды без парсера (failovermatch), и функция из parse.c у них не собралась бы. */
static inline const struct output *out_via(const struct spec *sp, const struct output *o) {
    if (!o->via[0]) return NULL;
    for (size_t i = 0; i < sp->out_n; i++)
        if (!strcmp(sp->out[i].name, o->via)) return &sp->out[i];
    return NULL;
}

/* Сколько переходов via от выхода до выхода без via: 0 — туннель идёт напрямую. Нужна тем, кто
 * обходит выходы в порядке зависимостей (сторож, супервизор помощников): цель — раньше
 * зависящего. Спека круги и цепочки длиннее MAX_VIA_DEPTH отвергает (via_check в spec.c);
 * предел здесь — только чтобы стенд, собирающий выходы в обход парсера, не зациклился. */
static inline int out_via_depth(const struct spec *sp, const struct output *o) {
    int d = 0;
    for (const struct output *t = out_via(sp, o); t && d < MAX_VIA_DEPTH; t = out_via(sp, t)) d++;
    return d;
}

/* Где лежит nfqws. Путь пакета zapret на OpenWrt (remittor/zapret-openwrt) — тот же, что у
 * оригинала bol-van. Литералом, а не поиском в PATH: у procd своё окружение, и «нашли не то»
 * здесь означало бы чужой процесс на нашей очереди. */
#define NFQWS_PATH "/opt/zapret/nfq/nfqws"

/* Поставить и снять правило маршрутизации выхода. Одна пара функций на четыре места, где эти
 * команды нужны (apply, уборка мёртвых правил, отказ выхода, привязка устройства): пока
 * команды стояли строками в каждом месте, добавление маски означало бы четыре шанса забыть
 * её в одном.
 *
 * rule_drop снимает ОБЕ формы — с маской и прежнюю без неё. Прежняя остаётся в ядре после
 * обновления движка, а два правила на одну метку дают порядок по приоритету, а не по
 * замыслу. Снимает в цикле: `ip rule add` дубликаты не проверяет, и копий может быть
 * несколько. */
void rule_add(unsigned mark, int table);
/* Снять установленные соединения выхода: следующий их пакет пройдёт обычным путём и получит
 * решение о маршруте заново. Нужна потому, что при включённой выгрузке потоков пакеты
 * установленного соединения до нашей цепочки не доходят — подробности у определения. */
void conntrack_evict(unsigned mark);
/* Снять записи conntrack, у которых (метка & mask) == val, — самим движком, через ctnetlink
 * (dnsd.c). Сколько снято, или -1, если разговора с ядром не вышло (нет сокета
 * NETLINK_NETFILTER или подсистемы conntrack в нём) — тогда conntrack_evict пробует внешний
 * инструмент. Живёт в dnsd.c, рядом с другим разговором с conntrack. */
int ctnl_evict_mark(uint32_t val, uint32_t mask);
/* Отметить выход «пущен напрямую» (on=1) или снять отметку — см. out_failopen_capable. */
void failopen_mark(const struct output *o, int on);
void rule_drop(unsigned mark, int table);
/* Правило выхода — ровно одной копией, НЕ СНИМАЯ стоящую: недостающее добавляется, лишние
 * копии и прежние формы снимаются уже после. Зачем так, а не rule_drop + rule_add, — у
 * определения в failover.c («без мгновения пустоты»). */
void rule_ensure(unsigned mark, int table);
/* Маршрут по умолчанию таблицы выхода — в устройство dev (или запрет, если dev == NULL) одной
 * заменой, без сброса таблицы; всё прочее в таблице снимается после. 0 — встал; иначе
 * таблица осталась, как была, и решать вызывающему. Там же, в failover.c. */
int table_bind(const struct output *o, const char *dev);

/* Привязать таблицу выхода к устройству: правило по метке и маршрут по умолчанию. Живёт в
 * failover.c, но нужна и клиенту VLESS: он привязывает своё устройство сам, потому что
 * только он знает момент, когда оно готово нести трафик. */
void bind_device(struct output *o, const char *dev);

/* Раскладка бит метки, реестр меток/таблиц, ход перебора узлов подписки, раскладка правил
 * старого ядра и шаблон временного файла — отдельными модулями (docs/architecture.md), но
 * включены здесь, чтобы ни один из файлов, читающих "spec.h", не пришлось трогать. */
#include "marks.h"
#include "probe.h"
#include "registry.h"
#include "nftcompat.h"
#include "jsonr.h"
#include "tmpfile.h"

#endif
