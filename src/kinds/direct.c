/* kind=direct — «не маршрутизировать»: трафик канала идёт обычным путём, как шёл бы без правила.
 *
 * Метки у выхода нет — заявлять пакет ничем не нужно, он и так идёт туда (out_needs_mark); нет
 * устройства, таблицы, помощника. Общий обход DPI здесь обязан работать — для этого прямой канал
 * и заводят, поэтому KC_SKIP_ZAPRET у него единственного не стоит (out_skips_zapret).
 *
 * Нулевой выход (kind == NULL) читается как этот вид — см. kind_of в spec.h. */
#include "spec.h"

const struct kind_ops kind_direct = {
    .name = "direct",
};
