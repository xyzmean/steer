/* xsteer: поддельное TCP-соединение поверх сырого сокета.
 *
 * ЗАЧЕМ ОТДЕЛЬНЫМ ФАЙЛОМ. Соединение нужно ОБЕИМ половинам — пиру и хабу, — и это ровно
 * тот случай, когда копия хуже общего кода: номера последовательности, отложенные
 * подтверждения и правило про «номер двигается только вперёд» одинаковы у обеих сторон, а
 * ошибка в любом из них означает молчаливую потерю пакетов. Сам файл о протоколе xsteer не
 * знает ничего: он переносит куски байтов, а что в них — дело xshake.c и вызывающего.
 *
 * ЧТО ЗДЕСЬ НЕ ПОВТОРЯЕТСЯ ИЗ obfs.c. Сборка и разбор сегмента, контрольная сумма и
 * арифметика номеров берутся оттуда (obfs.h) — они уже проверены стендом obfsmatch. Сырой
 * сокет, фильтры cBPF и правило против RST — тоже оттуда, вместе с четырьмя уроками,
 * записанными у их объявлений. Здесь только состояние соединения и цикл его поддержания.
 *
 * ГЛАВНЫЙ ИНВАРИАНТ. Смещение в потоке (seq минус начальный номер) служит nonce для AEAD,
 * поэтому он ОБЯЗАН двигаться только вперёд — даже когда отправка не удалась. Откат номера
 * ради «переиспользуем» означал бы повтор nonce, то есть полную потерю защиты. В obfs.c это
 * так по факту; здесь это несущая конструкция, и потому написано явно.
 */
#ifndef STEER_XSCONN_H
#define STEER_XSCONN_H
#include <errno.h>
#include <stdint.h>
#include <stddef.h>
#include <sys/socket.h>
#include "../obfs.h"
#include "xswire.h"

enum xs_conn_state { XSC_CLOSED, XSC_SYN_SENT, XSC_SYN_RCVD, XSC_EST };

struct xs_conn {
    uint32_t saddr, daddr;      /* сетевой порядок, как в пакете */
    uint16_t sport, dport;      /* хостовый порядок */
    uint32_t isn_tx, isn_rx;    /* начальные номера: наш и его */
    uint32_t seq, ack;          /* хостовый порядок */
    int      state;
    int      fd;                /* сырой сокет этого соединения */
    int      syn_tries;
    int      unacked;           /* принято сегментов с прошлого нашего подтверждения */
    long long born, last_rx, last_tx, last_ack;
    /* last_data_tx — когда мы последний раз отправили НАГРУЗКУ, а не голое подтверждение.
     *
     * Приговор о мёртвом пути обязан считаться от него, а не от last_tx, и разница не тонкая.
     * Голое подтверждение ничего не спрашивает: ответа на него не будет по построению — другая
     * сторона подтверждает подтверждения не больше, чем настоящий стек. Пока меркой было
     * «отправили после того, как получили», подтверждение считалось разговором, и сторона,
     * которая ТОЛЬКО ПОДТВЕРДИЛА принятое и замолчала, через XSC_DEAD_MS объявляла живой путь
     * мёртвым.
     *
     * Дороже всего это стоило на хабе половины на Go: он подтверждал последнюю запись пира,
     * дальше оба молчали (своего keepalive хаб на покое не шлёт — ему нечего сказать), и через
     * восемь секунд сессия освобождалась. Пир не узнавал об этом никак — RST на его следующую
     * запись не уходил, хаб просто перестал отвечать, — и переподнимал соединение по своей
     * тишине каждые двадцать-сорок секунд. У клиента та же беда на скачивании: принял,
     * подтвердил, поток кончился — и туннель пересоздавался на ровном месте. Поймано захватом
     * на живой связи роутер↔VPS, а не чтением. */
    long long last_data_tx;

    /* ---- то, что делает поток похожим на поток настоящего стека ---------------
     *
     * Все три величины найдены сравнением с НАСТОЯЩИМ трафиком xhttp (стенд
     * tests/xhttp-compare.sh в репозитории xsteer), а не выведены из соображений. Каждая
     * считается наблюдателем одной строкой и стоила нам отдельного признака.
     *
     * win — ОБЪЯВЛЯЕМОЕ окно. Стояло одно значение на все шесть тысяч пакетов; у настоящей
     * загрузки принимающая сторона показала 169 разных значений, отдающая — 36. Одно значение —
     * это не «похоже на TLS», это «вообще не похоже на стек TCP». Блуждает только у ПРИНИМАЮЩЕЙ
     * стороны и не чаще раза в восемь принятых сегментов: шевелить у обеих на каждом пакете
     * (первая версия) дало 2015 значений, то есть перестараться тут так же заметно.
     *
     * psh_run — PSH стоял на 99% пакетов вместо 16%. Настоящий стек ставит его на последнем
     * сегменте записи приложения, то есть примерно на каждом шестом при потоковой передаче.
     *
     * rx_since_ack — сколько принято сегментов с данными с прошлого ГОЛОГО подтверждения. У
     * настоящей загрузки обратное направление состоит В ОСНОВНОМ из голых подтверждений (59%
     * пакетов), а у нас их не было почти вовсе (0,2%): каждое внутреннее подтверждение TCP едет
     * как данные и заодно подтверждает принятое. Поэтому подтверждаем и отдельно — но не чаще
     * XSC_ACK_BARE_GAP_NS, потому что каждое такое подтверждение это системный вызов на горячем
     * пути приёма (без ограничения по частоте замерено минус 20% скорости). */
    /* tick_at и stall — учёт СВОЕЙ незанятости.
     *
     * tick_at — когда xs_conn_tick вызывали в прошлый раз; stall — сколько времени за ТЕКУЩУЮ
     * тишину наш собственный цикл вообще не исполнялся. Тишина сравнивается с XSC_DEAD_MS за
     * вычетом stall, поэтому «нам не давали процессор» больше не выглядит как «путь умер».
     * Обнуляется на любом принятом сегменте: запас относится к текущей тишине, а не копится за всё
     * время жизни соединения. */
    long long tick_at, stall;
    uint16_t win;
    uint64_t wrnd;
    int      win_fresh;
    int      psh_run;
    int      rx_since_ack;
    long long rx_total;
    long long last_bare_ns;
};

/* Шаг цикла и пороги. Значения взяты у obfs.c, кроме ACK_SEGS: там восемь выбраны из
 * экономии, а здесь двойка — из ПРАВДОПОДОБИЯ. Linux подтверждает примерно через сегмент, и
 * поток, подтверждающий раз в восемь, отличим от настоящего именно ритмом. Цена на обратном
 * пути загрузки — 40 байт на два сегмента по 1500, то есть 1,3%; при двустороннем трафике
 * голых подтверждений не нужно вовсе. */
#define XSC_TICK_MS      20
#define XSC_SYN_RETRY_MS 1000
#define XSC_SYN_RETRIES  6
#define XSC_ACK_SEGS     2
#define XSC_ACK_MS       40
/* Через сколько принятых сегментов с данными отправлять ГОЛОЕ подтверждение, даже если принятое уже
 * подтверждено попутно ушедшими данными, и не чаще какого промежутка. Три — как у настоящей
 * загрузки (1857 голых подтверждений на 6154 принятых сегмента); двести микросекунд — это пять
 * тысяч в секунду, то есть на реальных скоростях соотношение сохраняется, а на предельных
 * ухудшается, зато не стоит скорости. */
#define XSC_ACK_BARE        3
#define XSC_ACK_BARE_GAP_NS 200000
/* Границы блуждания объявляемого окна. Верх — предел поля; низ выбран так, чтобы при масштабе 7
 * даже он означал больше восьми мегабайт в полёте: ниже начинается риск, что conntrack по дороге
 * сочтёт наши сегменты недействительными (этот отказ уже стоил проекту потолка в 10 Мбит/с). */
#define XSC_WIN_FLOOR 63000
#define XSC_WIN_CEIL  65535
/* Тишина при АКТИВНОЙ отправке, после которой путь считается мёртвым.
 *
 * Первая версия ставила три секунды «чтобы быстрее замечать», и живой стенд показал, чем это
 * кончается: трафик односторонний (пир отправляет, отвечать нечем), хаб молчит, и пир раз
 * в три секунды поднимает соединение заново — то есть сама себе устраивает простой вместо
 * того, чтобы работать. Отсюда же следствие, без которого порог не помогает вовсе: хаб ОБЯЗАН
 * присылать keepalive, получив данные, — иначе односторонняя отправка всегда выглядит как
 * мёртвый путь. Так же устроен и WireGuard: keepalive посылает принимающая сторона.
 *
 * ПОЧЕМУ СОРОК ПЯТЬ СЕКУНД, А НЕ ПЯТНАДЦАТЬ. Пятнадцать — порог WireGuard (keepalive 10 с
 * плюс таймаут перездоровки 5 с), и он был здесь ровно по этой причине. На живом роутере он
 * сработал ЛОЖНО: два посторонних процесса заняли оба ядра, наш процесс получил 4% времени,
 * входящие сегменты пролежали в очереди дольше порога — и туннель пересоздался на ровном
 * месте. У WireGuard такой порог безопасен потому, что он не РАЗРУШАЮЩИЙ: там начинается новое
 * рукопожатие, а прежняя сессия продолжает нести трафик. У нас переподключение рвёт поддельное
 * соединение TCP, то есть стоит нового рукопожатия и проверки пути.
 *
 * ЧЕМ СОРОК ПЯТЬ ОКАЗАЛОСЬ НА САМОМ ДЕЛЕ. Ложное срабатывание, из-за которого порог поднимали,
 * произошло не в пути, а В НАС: наш процесс получил 4% времени, и тишина по стенным часам смешала
 * «путь молчит» с «мы не работали». Разделить их было нечем — теперь есть: tick замечает, что его
 * самого не вызывали, и это время в тишину не засчитывается (см. поля tick_at и stall). Смешения
 * больше нет, и порог можно вернуть к тому, что диктует сам протокол.
 *
 * А диктует он немного. При активной отправке обратный путь НЕ МОЛЧИТ ни секунды: хаб подтверждает
 * принятое голым ACK через XSC_ACK_MS (40 мс), и время последнего приёма обновляет любой принятый
 * сегмент, включая эти подтверждения. То есть при живом пути тишина измеряется десятками
 * миллисекунд, а не секундами, и восемь секунд — это двести пропущенных подтверждений подряд.
 * Запас оставлен на затор в мобильной сети, где ответ может опоздать на секунды.
 *
 * Наружу это даёт: пропавший интернет замечается за восемь секунд вместо сорока пяти. Ещё быстрее
 * его замечают два других признака — ошибка отправки (ENETUNREACH и родня, мгновенно) и изменение
 * адреса или маршрута (слежение за netlink, тоже мгновенно), — а этот порог закрывает единственный
 * оставшийся случай: путь цел с точки зрения ядра, но пакеты в него уходят в никуда.
 *
 * Порог обязан совпадать с реализацией на Go (link.DeadMS): по нему хаб выбирает срок вытеснения
 * (XSH_EVICT_QUIET_MS), и разойдись они — один и тот же пир на одном хабе принимался бы, а на
 * другом нет. */
#define XSC_DEAD_MS      8000
/* XSC_STALL_GAP_MS — с какого перерыва между вызовами tick мы считаем, что не работали МЫ, а не
 * путь. Секунда: исправный цикл пира тикает раз в двадцать миллисекунд, обслуживание хаба — раз в
 * сто, то есть на порядок чаще. Перерыв в секунду означает, что потоку не давали процессор — ровно
 * тот случай, из-за которого порог когда-то и подняли до сорока пяти секунд. */
#define XSC_STALL_GAP_MS 1000

long long xs_now_ms(void);

/* xs_path_gone — говорит ли ошибка отправки о том, что пути наружу больше НЕТ.
 *
 * Отдельно от прочих ошибок, потому что решение разное: здесь нет смысла ни повторять, ни ждать
 * тишины XSC_DEAD_MS — соединение надо поднимать заново, а перед этим дождаться, пока сеть
 * вернётся. Прежде такая ошибка была неотличима от любой другой и просто растила счётчик потерь:
 * при пропавшем интернете пир продолжал молотить в мёртвый сокет весь порог тишины.
 *
 * Сокет ПОДКЛЮЧЁН, то есть адрес источника у него закреплён при открытии, и после смены адреса он
 * не годится вовсе — его надо открывать заново, а не повторять отправку. EINVAL в списке именно
 * поэтому: так ядро отвечает на отправку с адреса, которого на машине больше нет.
 *
 * Тот же список, что в реализации на Go (link.ErrPathGone). */
static inline int xs_path_gone(int e) {
    return e == ENETUNREACH || e == ENETDOWN || e == EHOSTUNREACH ||
           e == EADDRNOTAVAIL || e == EINVAL;
}

/* MTU интерфейса, через который уходит трафик к адресу-источнику saddr, и его имя.
 *
 * Нужен обеим половинам: из него считается ВЕРХНЯЯ оценка MTU туннеля. Верхняя — потому что
 * путь может резать сильнее канала, и это выясняется только пробами (см. xswire.h). Живёт
 * здесь, а не в цикле пира, потому что то же самое считает хаб. */
int xs_egress_mtu(uint32_t saddr, char *ifname, size_t ifn);

/* Открыть соединение к хабу и отправить SYN. Возвращает 0 или -1. */
/* Открыть поддельное соединение. `shard` задаёт МЛАДШИЕ БИТЫ порта источника: порт выбираем мы
 * сами, а хаб раскладывает соединения по своим воркерам именно по этим битам
 * (obfs_filter_port_shard). Пир с несколькими соединениями обязана дать им РАЗНЫЕ значения —
 * иначе все его соединения достанутся одному воркеру хаба, и второе ядро там не заработает.
 * Отрицательное значение означает «любой порт». */
int xs_conn_open(struct xs_conn *c, uint32_t daddr, int dport, int shard);
void xs_conn_close(struct xs_conn *c);

/* Отправить служебный сегмент (SYN, ACK, RST) или короткую нагрузку. */
int xs_conn_send(struct xs_conn *c, uint8_t flags, const void *payload, size_t plen,
                 int with_mss);

/* Записать заголовок TCP ПЕРЕД нагрузкой, лежащей по row + XS_HDR_ROOM, и вернуть указатель
 * на начало сегмента. Копий нет: сегмент уходит с того места, где уже лежит шифротекст.
 * Двигает seq — то есть и смещение, из которого выводится nonce. */
uint8_t *xs_conn_ahead(struct xs_conn *c, uint8_t *row, size_t plen, size_t *seglen,
                       long long now);

/* Разложить готовую ЗАПИСЬ на сегменты для sendmmsg и двинуть номер на всю её длину.
 *
 * Зачем резать: у настоящего TLS запись живёт своей длиной и границ сегментов не соблюдает, а у нас
 * совпадали всегда — 100% против 0%, самый дешёвый признак, какой можно придумать (см. xswire.h).
 * Заодно это единственный способ увезти пачку кадров: она больше сегмента по построению.
 *
 * БЕЗ КОПИЙ: у каждого сегмента два куска — свой заголовок TCP из hdrs[i] и часть записи по
 * указателю в неё же. Ядро соберёт их само, sendmmsg это умеет.
 *
 * Смещение выдаётся ОДНО на всю запись (ею она и зашифрована) и тратится целиком: половина
 * записи, ушедшая при неудаче, не повод возвращать номер назад — это был бы повтор nonce.
 * Восстанавливать потерянные сегменты мы не будем: это UDP-туннель под видом TCP.
 *
 * Возвращает число сегментов или -1, если не влезло в cap. */
int xs_conn_split_mm(struct xs_conn *c, const uint8_t *rec, size_t rec_len, size_t max_seg,
                     uint8_t hdrs[][20], struct iovec *iov, struct mmsghdr *mm,
                     size_t cap, long long now);

/* Смещение, с которым уйдёт СЛЕДУЮЩИЙ пакет данных: им шифруется его же нагрузка.
 * Читается ДО xs_conn_ahead — она номер уже двинет. */
static inline uint32_t xs_conn_rel_next(const struct xs_conn *c) {
    return c->seq - c->isn_tx;
}

/* Смещение принятого сегмента. */
static inline uint32_t xs_conn_rel_of(const struct xs_conn *c, uint32_t seq) {
    return xs_rel(seq, c->isn_rx);
}

/* Учесть принятый сегмент: подтверждения, состояние, счётчики. Возвращает 1, если сегмент
 * несёт данные, которые надо разобрать, 0 — если служебный, -1 — если соединение закрылось
 * (пришёл RST). */
int xs_conn_on_seg(struct xs_conn *c, const struct obfs_seg *s, long long now);

/* Раз в тик: повторить SYN, отправить отложенное подтверждение, сказать про мёртвый путь.
 * Возвращает 1, если путь считается мёртвым и соединение надо поднимать заново. */
int xs_conn_tick(struct xs_conn *c, long long now, int sending);

#endif
