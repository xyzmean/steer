# args: apply --dry-run --spec TMP/spec-if.json --state-dir TMP/st-if
# exit: 0
table inet steer {
    set vpn_ip {
        type ipv4_addr
        flags interval
        auto-merge
        elements = { 149.154.160.0/20, 91.108.4.0/22 }
    }
    chain prerouting_mark {
        type filter hook prerouting priority mangle + 1; policy accept;
        iifname "br-lan" ip daddr @vpn_ip meta mark set mark and 0xf00fffff or 0x40100000 ct mark set mark counter return comment "steer:vpn_ip"
    }

    chain postrouting_down {
        type filter hook postrouting priority srcnat + 10; policy accept;
        oifname "br-lan" ip saddr @vpn_ip counter comment "steer-down:vpn_ip"
    }
    chain prerouting_dns {
        type nat hook prerouting priority dstnat; policy accept;
        iifname "br-lan" udp dport 53 counter redirect to :5300
        iifname "br-lan" tcp dport 53 counter redirect to :5300
    }
}
